{
  "name": "hello-world",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "express": "^5.0.0",
    "mcp-authkit": "workspace:*",
    "mcp-authkit-adapter-express": "workspace:*",
    "mcp-authkit-store-memory": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^25.9.0"
  }
}
